# TypeScript

- `witnesses.ts` — witness implementations and private-state helpers
- `simulator.ts` — in-memory harness threading contract state between participants
- `tournament_pass.test.ts` — lifecycle, access control, nullifier and adversarial cases

Run: `npm run typecheck` and `npm test`.
