node_modules/
dist/
build/
managed/
# Commit only the compiled contract's JavaScript, which the front end imports.
# Keys and circuits stay local.
!src/managed/
src/managed/*/*
!src/managed/*/contract/
target/
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/

# Local private state (holds contract secrets)
midnight-level-db/
.mn-contract-*.mjs

# Front end build output
web/dist/
api/dist/

# TypeScript incremental build state
*.tsbuildinfo

# Local assistant instructions
CLAUDE.md

# Phase 0 spike artefacts: keep the contract source and compiled JS, not the keys
spikes/managed/*/keys/
spikes/managed/*/zkir/
